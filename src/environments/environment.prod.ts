export const environment = {
  production: true,
  // Para pruebas locales en navegador se conserva localhost.
  // En un dispositivo físico cambia "localhost" por la IP local de tu Mac.
  apiUrl: 'http://localhost/perfumestore_api',
};
